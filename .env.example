# Keep OPENAI_API_KEY server-only. Never prefix it with VITE_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Use "mock" for the fully deterministic judge demo, or "openai" for live structured decisions.
VITE_AGENT_PROVIDER=mock
